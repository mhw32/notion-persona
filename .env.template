# Local Notion API token used by `ntn workers exec --local`.
# Hosted Notion Worker tool calls receive a Notion client from the Agent context.
NOTION_API_TOKEN=

# Notion database IDs for the three MVP management databases.
PERSONA_REGISTRY_DATABASE_ID=
DOCS_INDEX_DATABASE_ID=
PERSONA_RUNS_DATABASE_ID=
