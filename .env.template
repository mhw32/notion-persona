# Local Notion API token used by `ntn workers exec --local`.
# Hosted Notion Worker tool calls receive a Notion client from the Agent context.
NOTION_API_TOKEN=

# Notion database IDs for the MVP databases.
DOCS_DATABASE_ID=
PERSONAS_DATABASE_ID=
FEATURES_DATABASE_ID=
EXECUTIONS_DATABASE_ID=

# Optional. Required for private repos or higher GitHub API rate limits.
GITHUB_TOKEN=
